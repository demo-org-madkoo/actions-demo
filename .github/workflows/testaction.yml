name: test comp

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/test
        id: comp
        with:
          my_name: "@madkoo"
      - run: echo "${{steps.comp.outputs.greeting_message}}"
